<h1>Artist Info</h1>

<%= @artist.name %>

<p> <%= pluralize(@artist.song_count, "song") %></p>

<ul>
  <% @artist.songs.each do |song| %>
    <li>
      <%=link_to song.title, song_path(song) %>
    </li>
  <% end %>
</ul>
