<h1>Show Artist</h1>
<%= @artist.name %>

<h3><%= pluralize(@artist.songs.count, 'songs') %></h3>
<ul>
  <% @artist.songs.each do |s| %>
  <li><%= link_to s.title, song_path(s)  %></li>
  <% end %>
</ul>
