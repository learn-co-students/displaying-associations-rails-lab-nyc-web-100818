<%= form_for @song  do |f| %>

  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />
  <%= f.label :artist_id %>
  <%= f.collection_select :artist_id, @artists, :id, :name %>

  <%= f.submit %>
<% end %>
